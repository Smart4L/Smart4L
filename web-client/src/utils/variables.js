export const WS_URL = `ws://${window.location.hostname}:8082`
export const API_URL = `http://${window.location.hostname}:8081`
export const MAP_URL = `http://${window.location.hostname}:8081/tile/{z}/{x}/{y}.png`
export const PHOTO_URL = `http://${window.location.hostname}:8080`